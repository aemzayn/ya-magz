// Object.keys(person.social_media).map(key => ({
//   key,
//   value: person.social_media[key],
// }))

export default function getSocmed({ socmed }) {
  // if (typeof socmed !== Array) throw new Error('Params must be type of Array')
  // console.log(socmed)
}
